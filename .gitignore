.venv/
__pycache__/
.DS_Store

# audio: copyrighted commercial tracks — never commit
dataset/audio/*
!dataset/audio/.gitkeep
vae_roundtrip_out/

# generated/stale artifacts
*.bak
*.log
*.zip
dataset/review_sheet.xlsx
.gstack/

# training secrets — never commit the filled-in env (keep the .example)
docker/train.env
train.env
